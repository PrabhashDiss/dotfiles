super + v
    $HOME/bin/dmenu_scripts/txtcliphist sel

super + c
    $HOME/bin/dmenu_scripts/txtcliphist add

super + s
    $HOME/bin/screenshot.sh

super + shift + s
    $HOME/bin/screenshot.sh selection

super + w
    $HOME/bin/screenshot.sh window

super + x
    whisper
